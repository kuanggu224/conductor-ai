# AGENTS.md

## 1. 项目定位

Conductor 是一个面向软件项目全生命周期的 AI 项目执行与多智能体协作平台。

平台长期目标包括：需求理解、方案设计、任务拆解、多 Agent 协作执行、质量门禁、人工审批、交付验证、审计与回放。

当前阶段优先完善核心控制层、后端/API 执行链路和平台自身的管理控制台。当前暂未实现或暂缓的能力，不代表永久移出产品范围。

## 2. 文档层级

处理产品定位、架构和实现范围时，应区分以下层级：

1. 长期产品愿景：平台最终要解决什么问题。
2. 核心架构原则：长期稳定的设计约束。
3. 当前实现状态：当前代码已经完成什么、暂未完成什么。
4. 阶段性计划：近期准备新增、恢复、重构或移除什么。

不得根据当前版本缺失的功能，擅自缩小长期产品定位。
“暂时删除”“当前不实现”“优先聚焦”等表述，应视为阶段性决策，除非文档明确说明为永久调整。

修改 README、产品定位或核心架构说明前，应同时检查相关代码、当前状态文档、路线计划和近期提交，避免单一文档造成误判。

## 3. 核心架构原则

- 控制层与执行层分离。
- 只有控制层负责阶段推进、质量门禁和最终状态决策。
- Planner、Router、Runner 和专业 Agent 负责具体任务执行，不得自行改变项目阶段。
- 多 Agent 协作应围绕结构化任务、依赖和产物进行，不以自由聊天作为目标。
- Agent 声称任务完成不能作为完成依据，应优先使用测试、接口、产物、持久化结果或其他可验证证据。
- 重要操作应支持重试、返工、人工介入和审计。
- 新增能力应尽量接入现有控制、任务、验证和审计体系，避免形成旁路实现。

## 4. 工作方式

开始任务前：

- 先阅读与任务直接相关的代码和文档。
- 识别任务属于缺陷修复、功能开发、架构调整、文档更新还是阶段性实验。
- 复杂任务先给出简洁计划，再执行修改。
- 不确定的信息应通过代码、测试、配置或提交记录验证，不要仅凭文件名或旧文档推断。

执行过程中：

- 优先复用现有结构和抽象，不随意新增重复模块。
- 不为了快速完成而绕过控制层、状态管理、验证或审计机制。
- 不擅自扩大需求范围。
- 如发现文档与代码冲突，应明确指出，并按“代码事实、明确决策、文档说明”的顺序判断。
- 若修改会影响产品定位、核心架构或公共接口，应在结果中单独说明。

完成后：

- 运行与改动范围匹配的测试、编译或静态检查。
- 说明修改内容、验证结果、未解决问题和潜在风险。
- 功能行为、运行方式、架构边界或当前状态发生变化时，应同步更新对应文档。
- 不要为了让文档看起来一致而虚构未实现能力。

## 5. 文档维护规则

文档应随项目推进维护，但不是每次代码修改都需要更新全部文档。

必须考虑更新文档的情况：

- 产品定位或能力边界发生变化。
- 核心架构、模块职责或数据流发生变化。
- 启动、部署、配置或依赖方式发生变化。
- 新增或删除用户可见功能。
- 当前实现状态、阶段计划或已知限制发生明显变化。
- API、命令、配置项或操作流程发生变化。

通常不需要更新高层文档的情况：

- 不改变外部行为的小型缺陷修复。
- 纯内部重构且职责和使用方式不变。
- 格式、命名或注释调整。

更新文档时，应优先修改最接近事实来源的文档，不要在多个文件中复制同一大段内容。
如同一信息必须出现在多处，应保持一个主文档，并在其他位置引用或简述。

## 6. 代码与安全边界

- 不提交真实密钥、Token、密码、私有地址或敏感配置。
- 不执行破坏性命令，除非任务明确要求且风险已说明。
- 不随意删除看似未使用的模块；应先检查动态加载、配置引用、测试和文档。
- 不在未验证的情况下大规模重写核心控制逻辑。
- 修改公共 API、状态模型或持久化结构时，应考虑兼容性和迁移影响。
- 生成代码后必须尽可能运行验证，不以“理论上可行”代替结果。

## 7. 前端与交付范围说明

`frontend/` 是 Conductor 平台自身的管理、观测和人工控制界面。

平台自身存在前端，不等于当前版本已经恢复面向客户项目的完整业务前端交付能力。

当前阶段如果缺少前端开发 Agent、浏览器验收或全栈交付链路，应在“当前实现状态”中说明，不得据此把 Conductor 永久定义为后端/API-only 平台。

## 8. 输出要求

- 默认使用中文说明。
- 表达应明确区分：已实现、部分实现、规划中、暂缓和推测。
- 不夸大完成度，不把演示数据或 Mock 能力描述为真实生产能力。
- 发现历史决策、代码和文档不一致时，应主动提示，而不是静默选择其中一个。
